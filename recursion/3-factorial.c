#include "main.h"
/**
 *factorial - function that return the factorial of a given number
 *@n: pointer
 *Return: -1 if an error
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}

	else if (n == 0 || n == 1)
	{
		return (1);
	}
}

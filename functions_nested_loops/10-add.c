#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 *add- Adds two integers and returns the result
 *
 *@r: is for one integer to add
 *@p: is for the other integer to add
 *Return: the result of adding r + p
 */
int add(int r, int p)
{
r  = r + p;
return (r);
}
